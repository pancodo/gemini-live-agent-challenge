GCS_BUCKET_NAME=historian-1
GCP_PROJECT_ID=reference-tine-482314-c6

# ── Access Gate (budget protection) ────────────────────────────
# Leave empty for dev (no gate). Set same value across all services.
ACCESS_CODE=
